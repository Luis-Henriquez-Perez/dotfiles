#!/usr/bin/emacs --script 
https://www.reddit.com/r/emacs/comments/66gh2q/elisp_for_scripting/
;; https://www.reddit.com/r/emacs/comments/66gh2q/elisp_for_scripting/
(message "foo")
;; (require 'seq)
;; (let (wallpaper-dir wallpapers)
;;   (setq wallpaper-dir (expand-file-name "~/Images/wallpapers/"))
;;   ;; I want to set it to a random wallpaper that's not the current
;;   ;; one.  Also I want to keep track of the current wallpapers because
;;   ;; I prefer no repetitions.
;;   (setq wallpaper (seq-random-elt wallpapers))
;;   ;; the feh command.
;;   (message "foo")
;;   ;; (shell-command (format "feh --bg-scale '%s'" (expand-file-name file)))
;;   )
