(require 'emms-player-mpv)
(setq emms-player-list '(emms-player-mpv))

(setq emms-source-file-default-directory (expand-file-name "Music/" "~/"))
(setq emms-directory (expand-file-name "emms/" oo-cache-dir))
