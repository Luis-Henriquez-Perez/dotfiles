;; Hyperbole can make texts links.  When I was using and org configuration this
;; was done with org in a "top-down" approach.  By which I mean you try to make
;; everything into an org file so that you can utilize org links everywhere.
;; Hyperbole is more of a "bottom-up" approach where you try make existing
;; patterns into links.
(require 'hyperbole)

(provide 'oo-hyperbole-config)
