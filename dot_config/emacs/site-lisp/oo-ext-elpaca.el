;; There is no function, but try to make one.
;; Register a recipe.
(oo-register-recipe)

;; Install a package immediately
(oo-install-package)

;; Ensure all packages are at their correct branches and commit.
()
